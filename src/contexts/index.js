import { createContext } from "react";

const WeatherContext = createContext("");
const FavoriteContext = createContext("");
const SearchContext = createContext("");

export { WeatherContext,FavoriteContext,SearchContext };
